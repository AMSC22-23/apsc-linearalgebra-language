# Format all files:
```
clang-format -i -style=Google  --sort-includes objective*/**/*.cpp && clang-format -i -style=Google  --sort-includes demo*/**/*.cpp && clang-format -i -style=Google  --sort-includes shared*/**/*.hpp && clang-format -i -style=Google  --sort-includes preconditioners*/**/*.hpp && clang-format -i -style=Google  --sort-includes algorithms*/**/*.hpp
```

