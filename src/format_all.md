# Format all files:
```
clang-format -i -style=Google  --sort-includes demo*/**/*.cpp && clang-format -i -style=Google  --sort-includes lib/**/*.hpp && clang-format -i -style=Google  --sort-includes preconditioners*/**/*.hpp && clang-format -i -style=Google  --sort-includes algorithms*/**/*.hpp
```

